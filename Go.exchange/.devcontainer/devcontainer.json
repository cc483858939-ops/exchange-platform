{
  "name": "Go & MySQL App",
  "dockerComposeFile": [
    "../docker-compose.yml"
  ],
  "service": "app",
  "workspaceFolder": "/workspace",
  "shutdownAction": "stopCompose",
  "remoteUser": "vscode",
  "customizations": {
    "vscode": {
      "extensions": [
        "golang.go"
      ]
    }
  },
  "postCreateCommand": "sudo chmod 666 /var/run/docker.sock || true && git config --global user.name 'cc' && git config --global user.email 'cc483858939@gmail.com' && git config --global --add safe.directory /workspace && go env -w GOPROXY=https://goproxy.cn,direct"
}