package controllers

// The old embedding_v1 implementation was replaced by the V2 signal/profile,
// candidate, ranker, and selection files. Historical tests keep their existing
// helper names, but all production calls use the single rules_v4 pipeline.
