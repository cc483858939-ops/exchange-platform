# Production on Cloudflare Pages uses /api and a Pages Function proxy.
# Configure API_ORIGIN in Cloudflare Pages, not as a VITE_ variable.
# Local development falls back to /api, which Vite proxies to the local backend.
VITE_API_BASE_URL=
